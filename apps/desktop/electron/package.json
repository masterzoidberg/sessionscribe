{
  "name": "sessionscribe-electron",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "main": "dist/main.js",
  "scripts": {
    "build": "esbuild src/main.ts src/preload.ts --bundle --platform=node --outdir=dist --external:electron --external:electron-store --external:axios --external:ws --format=cjs",
    "dev": "pnpm build && electron ."
  },
  "dependencies": {
    "electron-store": "^8.1.0",
    "axios": "^1.6.0",
    "ws": "^8.14.0"
  },
  "devDependencies": {
    "electron": "^31.3.1",
    "cross-env": "^7.0.3",
    "esbuild": "^0.19.0"
  }
}